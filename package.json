{
  "name": "sentinel-e2e-and-tools",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "cy:open": "cypress open",
    "cy:run": "rimraf cypress/results && cypress run",
    "report:jira": "node scripts/report-to-jira.js cypress/results"
  },
  "devDependencies": {
    "cypress": "^13.13.0",
    "mochawesome": "^7.1.3",
    "rimraf": "^5.0.5"
  }
}